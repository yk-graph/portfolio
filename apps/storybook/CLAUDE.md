# apps/storybook — conventions

Facts (port, scripts, tsconfig refs) live in config files — read those.
This file holds only conventions.

- This app only DISPLAYS components from `packages/ui`. No app logic here.
- Co-locate stories next to their components in packages/ui.
- Follow the official Flat Config as-is; don't override it.

(All code and comments in English.)
