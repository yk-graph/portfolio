---
description: Update CC-oriented design docs based on recent implementation
disable-model-invocation: true
allowed-tools: Read, Edit, Grep, Glob
---

Review recent code changes and update the docs under `docs/`
following `.claude/rules/documentation.md`.

Steps:

1. Summarize what was implemented in this session.
2. Use the mapping table in documentation.md to find which doc(s) to touch.
   Most changes map to `docs/architecture.md`.
3. Update them, keeping facts that belong in code OUT of the docs.
4. If no update is needed, report "no update needed" with the reason.

All docs are in English.

Before editing, show WHICH docs and HOW, then proceed.
